# JMA Weather System 運用コマンド説明書

本システムを簡単に運用・管理できるようにするためのバッチファイルです。
ダブルクリックするだけで実行できます。

## 1. git-pull.bat
- **用途**: GitHub上の最新のコードや変更を、今お使いのPC（ローカル）に同期（取得）します。
- **実行タイミング**: 別の場所でコードを変更したときや、アップデートがあった際に実行してください。

## 2. git-push.bat
- **用途**: PC上（ローカル）で変更・編集したファイルを、そのままGitHubへアップロード（保存）します。
- **実行タイミング**: コードや設定を変更し、それを本番に反映させたい時に実行してください。
- **補足**: コミットメッセージは自動的に「update: 手動コミット」となります。

## 3. force-update.bat
- **用途**: Cloudflare KV（データベース）に保存されている気象庁の情報を一度リセットし、最新のデータを強制的に再取得します。
- **実行タイミング**: アプリ上のデータが古くなっていると感じた時や、情報がうまく表示されない時に実行してください。
